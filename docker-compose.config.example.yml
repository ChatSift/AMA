version: "3.7"

services:
  grafana:
    environment:
      GF_AUTH_GITHUB_CLIENT_SECRET: "CLIENT_SECRET_HERE"

  gateway:
    environment:
      CORDIS_AUTH: "beep boop"

  handler:
    environment:
      DISCORD_TOKEN: "beep boop boop"
