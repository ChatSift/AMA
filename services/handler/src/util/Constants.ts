export const EMOJI = {
  APPROVE: '🟩',
  DENY: '🟥',
  ABUSE: '🟧'
} as const;

Object.freeze(EMOJI);

export const COLORS = {
  APPROVED: 8450847,
  DENIED: 13967662,
  FLAGGED: 16753920
} as const;

Object.freeze(COLORS);
