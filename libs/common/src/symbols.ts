export const kLogger = Symbol('logger');

export const kSQL = Symbol('sql connection');

export const kRedis = Symbol('redis connection');

export const kRest = Symbol('cordis REST instance');

export const kConfig = Symbol('mapped env vars');
